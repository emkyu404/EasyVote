
const Home = ({onClick}) => {
    return (
        <div className='main-container' onClick={onClick}>
                <h1>Bienvenue sur la page d'accueil</h1>
        </div>
    )
}

export default Home